# Архитектура ClearEditor: v1.0 → v2.0

Сравнение (для новичков).

## Чем отличается v2.0 от v1.0?

| Что | v1.0 (было) | v2.0 (стало) |
|-----|-------------|---------------|
| **Структура `core/`** | плоская: все файлы в одной папке | разбита на `models/`, `views/`, `services/` |
| **Модели данных** | вперемешку с Qt-кодом | чистый Python, нет зависимости от Qt |
| **Логика вкладок** | в `TabPanel` (наследник `QTabWidget`) | разделена: `FileTabView` (UI) + `FileTabSrv` (логика) + `FileTabModel` (данные) |
| **Редактор** | `FileEditor` (содержит `HTMLEditor`) | `EditorWidget` (содержит `HTMLEditor`) — отделён от модели |
| **Тесты** | только ручные, визуальные | добавились модульные (assert-based) для моделей |

---

## Блок-схема: как это работает (v2.0)

```
┌─────────────────────────────────────────────────────────────┐
│                        main.py                              │
│   создаёт EditorApp, PluginManager, ErrorHandler            │
└─────────────────────┬───────────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                    EditorApp (QMainWindow)                  │
│  "Оркестратор" — владеет всеми компонентами                 │
│                                                             │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐   │
│  │  FileTabView │    │  FileTabSrv  │    │  MsgPanel    │   │
│  │  (вкладки)   │◄──►│  (логика)    │    │  (логи/ошиб) │   │
│  │  = QTabWidget│    │  = QObject   │    │  = QTabWidget│   │
│  └──────┬───────┘    └──────┬───────┘    └──────────────┘   │
│         │                   │                                │
│         │    ┌──────────────▼───────┐                       │
│         │    │    FileTabModel      │                       │
│         │    │  (список документов) │                       │
│         │    │  = чистый Python     │                       │
│         │    └──────────────────────┘                       │
│         │                                                   │
│  ┌──────▼──────────────────┐                                │
│  │  Для каждой вкладки:    │                                │
│  │  ┌──────────────────┐   │                                │
│  │  │  EditorWidget    │   │                                │
│  │  │  ├─ HTMLEditor    │   │                                │
│  │  │  │  ├─ HtmlHL    │   │                                │
│  │  │  └─ Document     │   │                                │
│  │  └──────────────────┘   │                                │
│  └─────────────────────────┘                                │
└─────────────────────────────────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                   PluginManager                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                   │
│  │ wordcount│  │textproc. │  │ iclear   │  ...               │
│  └──────────┘  └──────────┘  └──────────┘                   │
└─────────────────────────────────────────────────────────────┘
```

---

## Поток данных (на примере печати текста)

```
1. Пользователь печатает "Hello" в HTMLEditor
       │
2. HTMLEditor испускает cursorPositionChanged
       │
3. EditorWidget пересылает как cursor_position_changed
       │
4. FileTabSrv получает → испускает line_changed(int)
       │
5. EditorApp обновляет статус-бар: "Line: 1"
       │
   И одновременно:
       │
   HTMLEditor.document().modificationChanged
       │
   EditorWidget.modification_changed
       │
   FileTabSrv._on_modification_changed
       │
   FileTabSrv.editor_state_changed   ──→  EditorApp обновляет заголовок окна
```

## Что такое MVS?

**Model-View-Service** — упрощённый аналог MVC:

- **Model** (`core/models/`) — данные, без Qt. Можно тестировать без GUI.
- **View** (`core/views/`) — виджеты, только отображение и сигналы.
- **Service** (`core/services/`) — бизнес-логика, соединяет Model и View через сигналы.

Плагины (`plugins_service/`) используют похожую архитектуру, но в миниатюре.
