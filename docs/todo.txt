ЗАВЕРШЕНИЕ РЕФАКТОРИНГА

1. создать плагин WordCount - пример плагина подключенного к полю в QStatusBar (он был удален)
старая спецификация, сейчас декларация должна измениться:
Плагин 'WordCount' — подсчёт слов в QTextEdit.
   Декларация:
     status_fields = {"word_count": {"label_template": "Word: {}",
                                     "signal_name": "word_count_changed"}}
   Сигнал: word_count_changed(int)
    on_load(editor): получает EditorApp, подключается ко ВСЕМ вкладкам
     editor.tab_panel (textChanged), подписывается на tab_added
     (подключение к новым вкладкам) и currentChanged (пересчёт при
     переключении), запускает подсчёт.
   on_unload: отключается от всех textChanged, tab_added, currentChanged.
   menu_items/toolbar_items: не используются.

2. создать спецификации  plugins_service_spec.txt, file_tab_service_spec.txt в папке  docs/spec
и word_count_spec.txt в папке  docs/spec/plugins

3. В англоязычном CHANGELOG.md присутствует русское 'Диаграммы', заменить на Diagrams,
 и посмотри что бы там больше русского текста не было

