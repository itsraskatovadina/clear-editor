
--------------------------------------------------------------
PluginManager 
1. как организовать возможность обращения плагинов к функциональности друг друга?
например, плагин iclear может выбирать группу (список) файлов, если подключен плагин htmlprocessing 
- иметь возможность вызвать его метод validate_html для каждого файла из списка
2. формат декларации UI-элементов плагинов создавался в цейтноте. Удобна система переиспользования actions
и для меню и для тулбаров, ее надо оставить. А сам формат:
[{
        'kind': "menu", 'text': "Edit", 'content': [
            index_actions['remove_empty'], 
стоит заменить на что-то более стандартное/удобное. есть варианты?
3 variant_tool_submenu надо один оставить

--------------------------------------------------------------
Плагины
1. Плагин 'HTMLProcessing' переименовать в HTMLTools, перенести в меню Tools
2. Плагин 'TextProcessing' - Дополнение 
2.1. дополнение сервиса wrap, оборачивание произвольным тегом
- выбор тега, сохранение его до конца сеанса или до того как будет выбран другой тег, переменная selected_tag, меню 'select tag',
в начале сеанса selected_tag = div
- оборачивание выделенного текста выбранным тегом, при изменение тега, название меню изменяется  'wrap {selected_tag}'
2.2. добавляем простой сервис вставки шаблонов в плагин 'TextProcessing'
пункт меню 'insert a template' - после capitalize_first, toolbar_items — не используются
по нажатию динамически генерируется подменю, каждый пункт которого - один шаблон 
шаблоны хранятся в файле templates.json в папке плагина (вариант - в папке resourses/templates)
пример 
{
  "greeting": "Здравствуйте %%!",
  "goodbye": "До свидания.\nБлагодарим за общение."
}
плейсхолдеры %%  заменяются на выделенный текст, если текст не выделен - остается space, если больше одного %%  - все заменяются
4. Настройка видимости тулбаров и панелей
5. Спецификации плагинов переместить в отдельную папку и разделить

 
