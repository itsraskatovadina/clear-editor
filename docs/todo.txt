ОПТИМИЗАЦИЯ FileTabSrv

1. из метода add_tab выделить activate_tab, не будем пока оптимизировать,  просто выделим (причина - слишком длинный код метода).
 new_tab вызывается из EditorApp, он нужен
2. все методы начинающиеся с current_tab слить, все эти функции вызываеются из меню в EditorApp
для этого:
	2.1 из current_tab_view_property выделить view_property_tab(index), 
	view_property_tab нужен, чтобы скопировать полный путь к файлу (ничего дописывать не нужно, скопировать легко вручную), в дальнейшем можно размер, кодировку и т.д. добавить
	оптимизация по длине кода здесь с учетом планов получается не имеет смысла, ты прав, отменяем оптимизацию.
	2.2 в _reload_tab второй параметр убрать и вычислять внутри 
	2.3 все current_tab привести к одному виду: блок 
        index = self._view.currentIndex()
        if index == -1:
            return
     заменить на current_widget()
     2.4 все методы начинающиеся с current_tab заменить одним current_tab_process,
      Спасибо за замечание, не нужно от приватности отказываться. тогда тип обработки передавать как строку ("save", "save_as", "close", "reload", "property") и с match потом работать.
      более громоздко но безопасней.
     в EditorApp изменить формирование меню
3. _widgets мне кажется можно безболезненно удалить. заменить через  FileTabView _view.editor_at(index)
test_tab_moved в test_file_tab_srv.py нужно усложнить, тестовые вкладки должны иметь реальные пути, проверять, что после перемещения
 пути соответствуют правильному документу

по поводу замечания **П.3 _widgets → _view.editor_at(index)**
   В `_on_tab_moved` сейчас `self._widgets.insert(to_idx, ...pop(from_idx))`.
   После удаления _widgets эта строка убирается — QTabWidget сам
   переставляет виджеты при перемещении. Это верно?
Ответ: насколько я понимаю, QTabWidget сам перемещал вкладки независимо от _widgets и удаление этой строки ни на что не повлияет.
