

---------------------------------
По ConfigService
0. Обновить спецификацию, для этого просмотреть из docs/spec/ver1.0/ru/state_spec.txt, проверить соответствие коду,
на основе state_spec создать новую в docs/spec/plugins (предложи свой вариант названия) и дополнить следующими пунктами (нужно будет обсудить)

1. добавить валидацию config. 
1.1 валидация plugins_dir проходит, если: ключ присутствует, путь валиден для linux и windows, директория существует
    если валидация plugins_dir не проходит, тогда: 
    вариант А - приложение сообщает об этом в msg_panel и загружается без поддержки плагинов;
    вариант Б - приложение завершается с ConfigError.
вариант А мне больше нравится, но если он потребует серьезных доработок, тогда лучше выбрать вариант Б
1.2 валидация всех ключей в группе ui_defaults.
    если валидация группы ui_defaults не проходит, сообщаем и заменяем дефолтом.

2 создать тесты
(в этих тестах главное не проверка соответствия спецификации, а чтобы приложение не падало, предложи свои варианты тестов)
2.1 если config.json отсутствует или не парсится - завершение с ConfigError
2.2 проверка валидации config
2.3 если при запуске settings.ini нет - он создается и заполняется дефолтами, если пользователь не менял настройки
2.4 открытые и последние файлы сохраняются и восстанавливаются (проверить случай, если один из этих файлов удален или просто не открывается,
 валидность путей для linux и windows)
 
