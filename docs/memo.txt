АНАЛИЗ ЗАДАНИЯ (task.txt)
==========================

1. ЧТО НУЖНО СДЕЛАТЬ
---------------------

Пункт A — EditorApp: добавить get_status_bar() и get_menu_bar()
  Текущие прямые обращения в plugin_manager.py:
    149: QLabel(parent=editor.statusBar)
    150: editor.statusBar.addPermanentWidget(label)
    160: editor.menuBar().findChild(QMenu, entry['text'])
    162: editor.menuBar().addMenu(entry['text'])
    204: editor.statusBar.removeWidget(label)

  Решение:
    def get_status_bar(self): return self.statusBar
    def get_menu_bar(self): return self.menuBar()

  В plugin_manager.py заменить editor.statusBar → editor.get_status_bar(),
  editor.menuBar() → editor.get_menu_bar()

Пункт B — удалить wordcount
  Удалить папку plugins/wordcount/ целиком.
  В settings.ни plugins уже нет, но manifest.json и main.py остались
  в репозитории — их нужно удалить.

Пункт C — замена editor.tab_panel.currentWidget() на editor.current_editor()

  textprocessing/main.py:
    строки 62, 68, 82
    self._panel = editor.tab_panel  → не нужно
    editor.tab_panel.currentWidget().editor → editor.current_editor()

  htmlprocessing/main.py:
    строки 53, 89, 100, 103, 199, 202, 203
    editor.tab_panel.currentWidget().editor → editor.current_editor()
    ValErrorsDialog: принимает QTextEdit вместо FileEditor

Пункт D — остальное отложено
    После преобразования TabPanel+FileEditor → FileTabView+FileTabSrv


2. ЧТО ОСТАНЕТСЯ ПОСЛЕ ЭТОГО ШАГА (ДЛЯ ПАМЯТИ)
--------------------------------------------------
Файл                    | Ссылки
------------------------|-----------------------------------------------
iclear/main.py          | editor.tab_panel.tab_added (2 раза)
                        | editor.tab_panel.currentChanged
                        | editor.tab_panel.widget(index).path
iclear/wiclear.py       | parent.tab_panel.add_tab(Path(...))
htmlprocessing/main.py  | editor.msg_panel.new_view(...)
plugin_manager.py       | editor.addToolBar() / removeToolBar() / addDockWidget() / removeDockWidget()
                        | (стандартные методы QMainWindow — через editor)
EditorApp.on_message()  | self.msg_panel.new_message() — внутри EditorApp, ок

Это будет решаться после разделения на FileTabView + FileTabSrv.


3. ПОРЯДОК ДЕЙСТВИЙ
--------------------
1. EditorApp: добавить get_status_bar(), get_menu_bar()
2. PluginManager: editor.statusBar → editor.get_status_bar()
                   editor.menuBar() → editor.get_menu_bar()
3. Удалить plugins/wordcount/
4. Переписать textprocessing/main.py
5. Переписать htmlprocessing/main.py (+ ValErrorsDialog)
